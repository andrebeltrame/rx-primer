import styled from 'styled-components';

export const FilterToolbar = styled.header`
         margin: 0 0 30px 0;
         align-items: center;
         justify-content: center;
         & input,
         & select {
             margin: 0  20px 0px
         }
       `;

export default FilterToolbar;